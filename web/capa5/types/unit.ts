export class Unit {
    name: string;
    charterNumber: string;
    address: string;
    city: string;

    constructor(name: string, charterNumber: string, address: string, city: string) {
        this.name = name;
        this.charterNumber = charterNumber;
        this.address = address;
        this.city = city;
    }
}